{
  "rules": {
    ".read": true,
    ".write": true,
    "users": {
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    "recentCities": {
      ".indexOn": ["city", "id"]
    }
  }
}